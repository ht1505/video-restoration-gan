services:
  - type: web
    name: video-restoration-gan
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: WEIGHTS_PATH
        value: saved_models/generator.pth
